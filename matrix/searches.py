from .matrix_manip import Node

def solve(node):
        return 0
def hill_climb(node, iterations):
        return 0
def random_walk(node, iterations, restarts):
        return 0
def random_restart(node, iterations, prob):
        return 0
def annealing(node, iterations, temp, decay):
        return 0